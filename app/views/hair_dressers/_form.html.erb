<%= form_for(hair_dresser) do |f| %>
  <% if hair_dresser.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(hair_dresser.errors.count, "error") %> prohibited this hair_dresser from being saved:</h2>

      <ul>
      <% hair_dresser.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :logo_url %>
    <%= f.text_field :logo_url %>
  </div>

  <div class="field">
    <%= f.label :status %>
    <%= f.text_field :status %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
