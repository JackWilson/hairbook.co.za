<%= form_for(shop_stylist) do |f| %>
  <% if shop_stylist.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(shop_stylist.errors.count, "error") %> prohibited this shop_stylist from being saved:</h2>

      <ul>
      <% shop_stylist.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :shop_id %>
    <%= f.text_field :shop_id %>
  </div>

  <div class="field">
    <%= f.label :stylist_id %>
    <%= f.text_field :stylist_id %>
  </div>

  <div class="field">
    <%= f.label :start_date %>
    <%= f.date_select :start_date %>
  </div>

  <div class="field">
    <%= f.label :end_date %>
    <%= f.date_select :end_date %>
  </div>

  <div class="field">
    <%= f.label :status %>
    <%= f.text_field :status %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
